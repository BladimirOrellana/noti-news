
<div class="jumbotron section-header">
    <h1 class="title">Food</h1>
</div>
<div class="container-fluid">
    <div class="row">
       
{{#each data}}
 <div class="col-sm-12 col-md-6 col-lg-3">
            <div class="card card-dark bg-dark text-white">
  <img src="{{photo}}" class="card-img" alt="...">
  <div class="card-img-overlay">
    <h5 class="card-title primary">{{title}}</h5>
    <a class="card-text" href="{{link}}">Read</a>
    <a data-id="{{_id}}"  class="card-text id">save</a>
  </div>
</div>
        </div>
 
{{/each}}
   </div>
</div>